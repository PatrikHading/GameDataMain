spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gamedata
    username: root
    password: Pa55word
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      platform: mysql
